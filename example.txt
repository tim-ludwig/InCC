{
    fun counter(val) -> () -> val := val + 1;
    
    local f := (x, res) ->
        if x = 0 then {
            res
        } else {
            f(x - 1, x * res)
        }
    in fun fact(x) -> f(x, 1)
}