{
    counter := val -> inc -> val := val + inc;
    
    fac := local f := (x, res) ->
        if x = 0 then {
            res
        } else {
            f(x - 1, x * res)
        }
    in x -> f(x, 1)
}